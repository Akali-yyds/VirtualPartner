# Timeline 1.0 Archive

This archive is for traceability only. The active Unity project no longer uses timeline 1.0 as a Runtime format.

## Backup Point

- Preferred restore source: Git tag `stage2.4-approved`.
- Use this archive only as a quick reference when reading old implementation details.

## Archived Content

- `Runtime/`: old timeline 1.0 Runtime scripts moved from `VirtualPartner/Assets/VirtualPartner/Runtime/`.
- `Samples/Timelines/`: old timeline 1.0 JSON samples moved from `VirtualPartner/Assets/VirtualPartner/Timelines/`.

## Restore Guidance

To restore the old implementation for investigation, prefer checking out or diffing against `stage2.4-approved`.

Do not copy archived files back into `VirtualPartner/Assets/` unless a future stage explicitly decides to reintroduce timeline 1.0.
